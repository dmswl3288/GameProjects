using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class LVButtonGO : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public void OnLevel_1Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV1");
    }
    public void OnLevel_2Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV2");
    }
    public void OnLevel_3Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV3");
    }
    public void OnLevel_4Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV4");
    }
    public void OnLevel_5Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV5");
    }
    public void OnLevel_6Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV6");
    }
    public void OnLevel_7Clicked()
    {
        // 점수 저장 초기화
        ColorChange1._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange1._numBlue = 0;
        ColorChange1.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange2._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange2._numBlue = 0;
        ColorChange2.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange3._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange3._numBlue = 0;
        ColorChange3.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange4._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange4._numBlue = 0;
        ColorChange4.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange5._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange5._numBlue = 0;
        ColorChange5.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange6._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange6._numBlue = 0;
        ColorChange6.timer = 0.0f;

        // 점수 저장 초기화
        ColorChange._score = 0;
        // 클리어 판별을 위한 블루버튼개수 
        ColorChange._numBlue = 0;
        ColorChange.timer = 0.0f;

        SceneManager.LoadScene("LV7");
    }

    public void OnBackTitleButton()
    {
        SceneManager.LoadScene("Title");
    }
}
